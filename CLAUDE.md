# CLAUDE.md

本文件为 Claude Code 提供这个仓库的上下文与协作规则。

## 仓库性质

这是一个**学习仓库**，用于从零学习并实践 **Python / FastAPI / LangChain / 前端 AI 应用**。
不是生产项目，重点是「理解原理 + 动手实践 + 沉淀笔记」。

## 目录结构

```
python_tutorial/
├── docs/                      # 学习笔记与教程（按模块分层）
│   ├── 教程大纲.md            # 总目录：学什么
│   ├── 学习路线图.md          # 路线图：顺序、时间、里程碑
│   ├── README.md              # docs 索引
│   └── 00-环境搭建/ … 09-项目实战/   # 10 个模块，各自有 README
├── projects/                  # 练习代码，<模块编号>-<名字>
├── AGENTS.md                  # Codex 用
└── CLAUDE.md                  # 本文件
```

## 技术约定

- **包管理用 `uv`**：建项目 `uv init`、加包 `uv add`、运行 `uv run`。不要建议 `pip install` 到全局。
- 技术栈：Python → FastAPI → LangChain → Streamlit/Next.js。
- 笔记/代码注释默认中文。

## 协作规则

1. **讲解优先**：这是学习场景，解释「为什么」比直接给答案更重要。先讲原理、给最小示例，再让用户自己扩展。
2. **对应用户水平**：先查 `docs/学习路线图.md` 判断用户当前阶段，不要越过其已学内容讲解（避免引入未学概念）。
3. **主动沉淀笔记**：学完一个知识点，用 `study-note` 技能生成规范笔记，落到对应模块目录（`docs/<模块>/<章节号>-<主题>.md`）。
4. **练习与笔记分离**：代码放 `projects/`，笔记放 `docs/`。
5. **报错时定位根因**：给出报错含义、根因、修复思路，并引导用户理解，而不是只给补丁。
6. **章节知识点速查**：每进入一个新的学习章节，就在 `projects/<模块>/<章节>/README.md` 里补充「知识点速查」节，内容含：概念解释（尤其 range、切片这类易混概念）、操作方法速查表（如列表/字典的常用方法）、易错点、进阶提示。格式与 `projects/01-python基础/01.1~01.3` 的 README 保持一致；用户没学到的章节，进入时自动补，不必等用户提醒。

## 可用的技能（Skills）

- `study-note`：按规范模板生成学习笔记并自动归类到正确模块目录。用户说「记录笔记 / 总结这一课 / 写学习笔记」时使用。

## 常用任务

| 用户说 | 做法 |
|--------|------|
| "学 XX" | 读对应模块 README，讲原理 + 最小示例 |
| "建项目 / 加个包" | 用 `uv init` / `uv add`，并解释 |
| "记一下这节" | 调 `study-note` 技能 |
| "这个报错" | 解释 + 根因 + 思路 |
