# AGENTS.md — Codex 辅助学习指南

> 本文件告诉 Codex 如何在这个「Python / FastAPI / LangChain 学习仓库」里配合你学习。

## 项目背景

这是一个**学习仓库**（不是生产项目），用于从零学习并实践：

- **Python**（语言基础 + 进阶）
- **FastAPI**（后端 API）
- **LangChain / LangGraph**（AI 编排，含 RAG / Agent / 多 Agent 工作流）
- **前端 AI 应用**（Streamlit / Next.js）

**包管理统一用 `uv`**，不直接用 `pip install`。

## 目录约定

```
python_tutorial/
├── docs/                      # 学习笔记与教程（按模块分层）
│   ├── 教程大纲.md            # 总目录：学什么
│   ├── 学习路线图.md          # 路线图：怎么学、顺序、里程碑
│   ├── 00-环境搭建/ … 09-项目实战/   # 10 个模块，各自有 README
├── projects/                  # 练习代码，按 <模块编号>-<名字> 命名
├── AGENTS.md                  # 本文件（Codex 用）
└── CLAUDE.md                  # Claude Code 用
```

## Codex 的工作规则

1. **先读大纲再动手**：涉及学习内容时，先看 `docs/教程大纲.md` 与对应模块的 `README.md`，确保讲解范围一致。
2. **用 uv 管理依赖**：建项目用 `uv init`、加包用 `uv add`、运行用 `uv run`。**永远不要**让用户 `pip install` 到全局环境。
3. **解释优先于给答案**：这是学习仓库，遇到报错或问题，先帮用户**定位原因 + 讲清原理**，再给代码，而不是直接贴最终答案。鼓励用户自己补全。
4. **记录笔记**：学习完一个知识点，主动提议把要点写成笔记，落到对应模块目录（命名 `<章节号>-<主题>.md`），格式见下方模板。
5. **练习代码进 `projects/`**：示例、练习代码放到 `projects/<模块编号>-<名字>/`，与 `docs/` 笔记分离。
6. **中文交流**：默认用中文回复，代码注释也可用中文。

## 学习笔记模板

```markdown
# <章节号> · <主题>

## 核心概念
（这个知识点是什么、解决什么问题）

## 关键代码
```python
# 最小可运行示例
```

## 运行结果
（贴上实际输出）

## 常见坑
- （踩过的坑 + 解决办法）

## 下一步
（关联到后续章节/模块）
```

## 常见任务示例

| 用户说 | 你应该做 |
|--------|----------|
| "学 FastAPI 的依赖注入" | 先读 `docs/03-fastapi基础/README.md`，讲 `Depends` 原理 + 给最小示例 |
| "帮我建个项目" | `uv init` + `uv add`，并解释每一步 |
| "这个报错怎么回事" | 解释报错含义 + 根因 + 修复思路 |
| "记录一下这节笔记" | 按上面模板生成笔记，存到对应模块目录 |
